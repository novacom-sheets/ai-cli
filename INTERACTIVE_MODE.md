# Интерактивный режим AI CLI

Интерактивный режим позволяет вести непрерывный диалог с AI с сохранением контекста между запросами.

## Запуск интерактивного режима

### Базовый режим (без выполнения команд)

```bash
ai-cli chat
```

или просто:

```bash
ai-cli
```

### С поддержкой выполнения команд 🔥 НОВОЕ

```bash
ai-cli -x chat
```

## Возможности интерактивного режима

### 1. Сохранение контекста ✅

Весь диалог сохраняется, AI помнит предыдущие сообщения:

```
You: объясни что такое декораторы в python
Assistant: [объяснение декораторов]

You: покажи пример
Assistant: [показывает пример, помня что говорили о декораторах]

You: а как применить это для кеширования?
Assistant: [объясняет применение, используя контекст]
```

### 2. Переключение режима выполнения команд ⚡

В интерактивном режиме можно включать/выключать выполнение команд:

```
You: /execute
⚡ Command execution is now ENABLED

You: как показать список файлов
Assistant: [дает команду ls]

📋 Found 1 command(s)

🔧 Command to execute:
   ls

Execute this command? [y/n/q]: y
[выполняется команда]

You: /execute
⚡ Command execution is now DISABLED
```

### 3. Специальные команды

| Команда | Описание |
|---------|----------|
| `exit` или `quit` | Выход из интерактивного режима |
| `help` | Показать справку |
| `/execute` или `/x` | Переключить режим выполнения команд |

## Примеры использования

### Пример 1: Обучение с выполнением

```bash
$ ai-cli -x chat

AI CLI - Interactive Mode
Type 'exit' to quit, 'help' for commands
⚡ Command execution is ENABLED
------------------------------------------------------------

You: как найти все Python файлы в текущей директории
Assistant: Используйте команду find:

```bash
find . -name "*.py"
```

📋 Found 1 command(s)

🔧 Command to execute:
   find . -name "*.py"

Execute this command? [y/n/q]: y

▶ Executing: find . -name "*.py"
------------------------------------------------------------
./main.py
./tests/test_app.py
./utils/helper.py
✓ Command completed successfully
------------------------------------------------------------

You: а как подсчитать количество строк в них?
Assistant: Используйте wc с xargs:

```bash
find . -name "*.py" -exec wc -l {} +
```

[диалог продолжается с контекстом]
```

### Пример 2: Разработка с контекстом

```bash
$ ai-cli chat

You: я хочу создать REST API на Flask
Assistant: [объясняет основы Flask REST API]

You: покажи структуру проекта
Assistant: [показывает структуру папок]

You: напиши код для endpoint /users
Assistant: [генерирует код]

You: а как добавить валидацию?
Assistant: [добавляет валидацию, помня код из предыдущего ответа]
```

### Пример 3: Отладка с командами

```bash
$ ai-cli -x chat

You: у меня ошибка в приложении, как диагностировать?
Assistant: Давайте проверим логи:

```bash
tail -n 50 app.log
ps aux | grep python
```

[команды выполняются с подтверждением]

You: вижу что процесс падает, что дальше?
Assistant: [продолжает диагностику с учетом результатов команд]
```

## Режимы работы

### Режим 1: Только диалог

```bash
ai-cli chat
```

**Функции:**
- ✅ Сохранение контекста
- ✅ Непрерывный диалог
- ❌ Выполнение команд

**Использование:**
- Обучение и объяснения
- Генерация кода
- Консультации

### Режим 2: Диалог + выполнение команд

```bash
ai-cli -x chat
```

**Функции:**
- ✅ Сохранение контекста
- ✅ Непрерывный диалог
- ✅ Выполнение команд с подтверждением
- ✅ Переключение режима на лету (`/execute`)

**Использование:**
- Обучение с практикой
- Диагностика проблем
- Автоматизация задач
- Пошаговые инструкции

## Workflow примеры

### Создание нового проекта

```bash
$ ai-cli -x chat

You: помоги создать Python проект для CLI приложения
Assistant: [объясняет структуру]

You: создай структуру папок
Assistant:
```bash
mkdir -p myproject/{src,tests,docs}
cd myproject
```

[выполняется с подтверждением]

You: создай setup.py
Assistant: [генерирует setup.py]

You: теперь создай main.py с argparse
Assistant: [генерирует main.py]
```

### Диагностика проблемы

```bash
$ ai-cli -x chat

You: приложение работает медленно, что проверить?
Assistant: Давайте проверим:

```bash
ps aux | grep python
free -m
df -h
```

[выполняются команды, AI видит результаты в контексте]

You: вижу что памяти мало, что делать?
Assistant: [дает рекомендации на основе результатов]
```

### Обучение новой технологии

```bash
$ ai-cli chat  # без -x для безопасности

You: объясни Docker для начинающих
Assistant: [объясняет основы]

You: покажи пример Dockerfile
Assistant: [показывает пример]

You: а что такое docker-compose?
Assistant: [объясняет, помня контекст]

You: покажи пример docker-compose.yml для Python + Postgres
Assistant: [генерирует пример, учитывая предыдущее объяснение]
```

## Советы по использованию

### 1. Используйте контекст

AI помнит весь диалог, не нужно повторять информацию:

❌ Плохо:
```
You: объясни декораторы
Assistant: [объяснение]

You: объясни кеширование с декораторами в python
```

✅ Хорошо:
```
You: объясни декораторы
Assistant: [объяснение]

You: как использовать их для кеширования?
```

### 2. Включайте выполнение осторожно

Запускайте с `-x` только когда планируете выполнять команды:

```bash
# Для обучения и консультаций
ai-cli chat

# Для практики и выполнения
ai-cli -x chat
```

### 3. Используйте /execute для переключения

Можно начать без выполнения и включить позже:

```
You: объясни как найти файлы
Assistant: [объяснение команды find]

You: /execute
⚡ Command execution is now ENABLED

You: теперь покажи как найти все .py файлы
Assistant: [показывает команду]
[команда выполняется]
```

### 4. Переспрашивайте и уточняйте

AI помнит контекст, можно задавать уточняющие вопросы:

```
You: как работает git rebase?
Assistant: [объяснение]

You: а в чем разница с merge?
Assistant: [сравнение, используя контекст]

You: покажи пример когда лучше использовать rebase
Assistant: [конкретный пример]
```

## Отличия от однократных запросов

| Возможность | Интерактивный режим | Однократный запрос |
|-------------|---------------------|-------------------|
| Сохранение контекста | ✅ Да | ❌ Нет |
| Уточняющие вопросы | ✅ Да | ❌ Нет |
| Выполнение команд | ✅ С подтверждением | ✅ С подтверждением |
| Переключение режимов | ✅ `/execute` | ❌ Нет |
| Быстрый ответ | ❌ Нет | ✅ Да |

## Комбинирование режимов

### Интерактивный для планирования + Однократные для выполнения

```bash
# Планирование в интерактивном режиме
$ ai-cli chat

You: помоги спланировать структуру проекта
Assistant: [обсуждение, планирование]

You: exit

# Генерация файлов однократными запросами
$ ai-cli -s "create README.md for project"
$ ai-cli -s -c "create main.py with structure"
$ ai-cli -s "create requirements.txt"
```

## Горячие клавиши

| Клавиши | Действие |
|---------|----------|
| `Enter` | Отправить сообщение |
| `Ctrl+C` | Выход из программы |
| `Ctrl+D` | Выход (EOF) |

## Ограничения

### 1. Память ограничена размером контекста

Очень длинные диалоги могут привести к забыванию начальных сообщений.

**Решение:** Начните новую сессию для новой темы.

### 2. Команды выполняются в новых shell

Каждая команда в новом процессе:

```bash
You: cd /tmp
[выполняется]

You: pwd
[покажет исходную директорию, не /tmp]
```

**Решение:** Используйте `cd /tmp && pwd` в одной команде.

### 3. Интерактивные команды не работают

Команды требующие ввода не поддерживаются:

```bash
# НЕ работает:
You: запусти vim
```

## FAQ

### Q: Как очистить контекст?

A: Выйдите и запустите заново:
```bash
You: exit
$ ai-cli chat
```

### Q: Можно ли сохранить историю диалога?

A: Пока нет автоматического сохранения. Копируйте важные части вручную.

### Q: Как отменить команду во время выполнения?

A: Нажмите `Ctrl+C` (но это завершит весь сеанс).

### Q: Работает ли выполнение команд на Windows?

A: Частично. Лучше использовать WSL.

### Q: Можно ли переключать модели в интерактивном режиме?

A: Пока нет, модель выбирается при запуске.

## Заключение

Интерактивный режим - мощный инструмент для:
- 📚 Обучения с сохранением контекста
- 🔧 Пошаговой диагностики проблем
- 💡 Разработки с итеративным уточнением
- 🚀 Практики с безопасным выполнением команд

**Попробуйте:**

```bash
# Начните диалог с выполнением команд
ai-cli -x chat

# Задайте вопрос
You: помоги настроить git для проекта

# Следуйте инструкциям с подтверждением команд
# Уточняйте и переспрашивайте
# Выйдите когда закончите: exit
```

---

**См. также:**
- `COMMAND_EXECUTION.md` - Выполнение команд
- `USAGE.md` - Общее руководство
- `CHEATSHEET.md` - Быстрая справка