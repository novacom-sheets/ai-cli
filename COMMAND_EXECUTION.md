# Выполнение команд с AI CLI

AI CLI теперь может автоматически извлекать и выполнять shell-команды из ответов AI с вашего согласия!

## Быстрый старт

```bash
# С подтверждением (безопасно)
ai-cli -x "как показать список файлов"

# С автоподтверждением (используйте осторожно)
ai-cli -x -y "покажи свободное место на диске"
```

## Как это работает

1. **AI генерирует ответ** с командами в markdown блоках
2. **CLI извлекает команды** из блоков ```bash или ```sh
3. **Проверяет безопасность** команд
4. **Запрашивает подтверждение** у пользователя
5. **Выполняет команды** и показывает результат

## Опции

### --execute / -x

Включает режим выполнения команд с подтверждением:

```bash
ai-cli --execute "how to list python files"
ai-cli -x "show disk usage"
```

**Что происходит:**
1. AI генерирует ответ с командами
2. CLI находит все команды в блоках ```bash
3. Для каждой команды спрашивает подтверждение:
   ```
   🔧 Command to execute:
      ls *.py

   Execute this command? [y/n/q] (y=yes, n=skip, q=quit):
   ```
4. Выполняет подтвержденные команды

### --yes / -y

Автоматически подтверждает все команды (используйте с осторожностью!):

```bash
ai-cli -x -y "create backup directory"
ai-cli -xy "show system info"  # короткая форма
```

**⚠️ Внимание:** Используйте `-y` только для безопасных операций!

## Примеры использования

### Базовые команды

```bash
# Список файлов
ai-cli -x "как показать все файлы включая скрытые"

# Информация о системе
ai-cli -x "покажи информацию о процессоре"

# Сетевые команды
ai-cli -x "как проверить открытые порты"

# Поиск файлов
ai-cli -x "найди все Python файлы в текущей директории"
```

### Работа с файлами

```bash
# Создание директорий
ai-cli -x "создай структуру папок для проекта"

# Поиск и фильтрация
ai-cli -x "найди все файлы больше 100MB"

# Архивирование
ai-cli -x "как создать tar.gz архив текущей папки"
```

### Системная информация

```bash
# Использование диска
ai-cli -x "покажи свободное место на диске"

# Память
ai-cli -x "сколько памяти использует система"

# Процессы
ai-cli -x "покажи топ процессов по CPU"
```

### Git операции

```bash
# Статус репозитория
ai-cli -x "покажи статус git репозитория"

# История коммитов
ai-cli -x "последние 5 коммитов с краткими сообщениями"

# Ветки
ai-cli -x "список всех веток"
```

### Docker команды

```bash
# Контейнеры
ai-cli -x "покажи все запущенные docker контейнеры"

# Образы
ai-cli -x "список docker образов с размерами"

# Логи
ai-cli -x "как посмотреть логи контейнера"
```

## Интерактивное подтверждение

При использовании `-x` без `-y`, CLI спрашивает подтверждение для каждой команды:

```
🔧 Command to execute:
   mkdir -p project/{src,tests,docs}

Execute this command? [y/n/q] (y=yes, n=skip, q=quit):
```

**Опции:**
- `y` или `yes` - Выполнить команду
- `n` или `no` - Пропустить команду
- `q` или `quit` - Отменить выполнение всех оставшихся команд

## Безопасность

CLI автоматически блокирует потенциально опасные команды:

### ❌ Блокируемые паттерны:

```bash
rm -rf /              # Удаление корневой директории
rm -rf *              # Удаление всех файлов
dd if=... of=/dev     # Запись на устройство
mkfs.*                # Форматирование диска
sudo rm               # sudo удаление
curl ... | bash       # Pipe в shell
wget ... | sh         # Pipe в shell
```

### ⚠️ Пример блокировки:

```bash
$ ai-cli -x -y "delete all files in root"

📋 Found 1 command(s) to execute:
============================================================

⚠️  Command 1: BLOCKED (unsafe)
   rm -rf /
   Reason: Potentially dangerous command detected
```

## Вывод выполнения

Для каждой команды CLI показывает:

```
▶ Executing: ls -la
------------------------------------------------------------
total 24
drwxr-xr-x  5 user  staff  160 Jan  6 10:00 .
drwxr-xr-x  3 user  staff   96 Jan  5 12:00 ..
-rw-r--r--  1 user  staff  123 Jan  6 09:00 file.txt

✓ Command completed successfully
------------------------------------------------------------
```

**Если команда fails:**

```
▶ Executing: cat nonexistent.txt
------------------------------------------------------------
stderr: cat: nonexistent.txt: No such file or directory

✗ Command failed with exit code 1
------------------------------------------------------------
```

## Итоговая статистика

После выполнения всех команд:

```
============================================================
Summary: Executed 3/4 commands
         Success: 2/3
```

## Практические примеры

### Создание проекта

```bash
ai-cli -x "создай структуру Python проекта"
```

AI может вернуть:
```bash
mkdir -p myproject/{src,tests,docs}
cd myproject
touch README.md
touch requirements.txt
touch setup.py
```

Вы подтверждаете каждую команду и получаете готовую структуру.

### Диагностика системы

```bash
ai-cli -x "check system health"
```

AI может вернуть:
```bash
df -h
free -m
ps aux | head -10
netstat -tuln | grep LISTEN
```

### Backup операции

```bash
ai-cli -x "create backup of documents folder"
```

AI может вернуть:
```bash
tar -czf documents_backup_$(date +%Y%m%d).tar.gz ~/Documents
ls -lh documents_backup*.tar.gz
```

### Поиск и анализ

```bash
ai-cli -x "find large files in current directory"
```

AI может вернуть:
```bash
find . -type f -size +100M -exec ls -lh {} \;
du -sh */
```

## Комбинирование с другими опциями

### Сохранение вывода + выполнение

```bash
# Сохранить инструкции и выполнить команды
ai-cli -x -o instructions.md "setup nodejs project"
```

### Авто-save + выполнение

```bash
# Создать скрипт и выполнить команды из ответа
ai-cli -s -x "create backup.sh script"
```

## Workflow примеры

### Быстрая диагностика

```bash
# 1. Спросить AI
ai-cli -x "система тормозит, что проверить?"

# 2. Подтвердить команды
# 3. Получить результаты
# 4. Действовать на основе информации
```

### Обучение командам

```bash
# Узнать команду и сразу попробовать
ai-cli -x "как рекурсивно найти все TODO в коде"

# AI покажет команду
# Вы выполните её с подтверждением
# Увидите результат
```

### Автоматизация рутины

```bash
# Создать alias для частых задач
alias backup="ai-cli -x -y 'create timestamped backup of current directory'"
alias clean="ai-cli -x 'show how to clean system cache'"
```

## Ограничения

### Таймаут команд

Команды ограничены **30 секундами** выполнения:

```
✗ Command timed out after 30 seconds
```

Для длительных операций используйте прямое выполнение.

### Интерактивные команды

Команды требующие ввода **не поддерживаются**:

```bash
# НЕ работает:
ai-cli -x "run interactive python shell"
```

Используйте только неинтерактивные команды.

### Контекст выполнения

Каждая команда выполняется в **новом shell**:

```bash
cd /tmp        # Выполнено
pwd            # Покажет оригинальную директорию, не /tmp!
```

Для связанных команд используйте `&&`:

```bash
cd /tmp && ls  # Работает
```

## Best Practices

### 1. Начинайте с подтверждением

```bash
# ✓ Безопасно - спросит подтверждение
ai-cli -x "install packages"

# ✗ Опасно - выполнит автоматически
ai-cli -x -y "install packages"
```

### 2. Проверяйте команды перед выполнением

Читайте что предлагает AI перед подтверждением.

### 3. Используйте для обучения

```bash
# Узнайте команду и попробуйте безопасно
ai-cli -x "как найти большие файлы"
```

### 4. Комбинируйте с сохранением

```bash
# Сохраните инструкции на будущее
ai-cli -x -o howto.md "docker debugging commands"
```

### 5. Не используйте для критичных операций

Для важных операций (production, базы данных) используйте команды напрямую.

## Часто задаваемые вопросы

### Почему команда не найдена?

CLI ищет команды в markdown блоках:

```markdown
```bash
ls -la
```
```

Если AI не обернул команду в блок - она не будет найдена.

### Можно ли отключить проверку безопасности?

Нет, проверка безопасности всегда активна для защиты системы.

### Как выполнить команду требующую sudo?

```bash
# Не используйте -y, чтобы видеть команду
ai-cli -x "как обновить систему через apt"

# Подтвердите и введите пароль sudo вручную
```

### Работает ли с PowerShell (Windows)?

Частично. CLI извлекает только bash/sh команды. Для Windows используйте WSL.

### Можно ли выполнять Python код?

Нет, только shell команды. Для Python используйте:

```bash
ai-cli -s -c "create script.py"
python script.py
```

## Примеры на разных языках

### Русский

```bash
ai-cli -x "покажи все Python файлы"
ai-cli -x "создай папку backup"
ai-cli -x "сколько места занимает папка Documents"
```

### English

```bash
ai-cli -x "show all log files"
ai-cli -x "check if port 3000 is open"
ai-cli -x "find files modified today"
```

## Расширенные примеры

### Pipeline команды

```bash
ai-cli -x "find and count python files by extension"
```

Может вернуть:
```bash
find . -name "*.py" | wc -l
find . -name "*.py" -exec du -ch {} + | tail -1
```

### Условные команды

```bash
ai-cli -x "check if docker is running and show containers"
```

Может вернуть:
```bash
docker ps > /dev/null 2>&1 && docker ps -a || echo "Docker not running"
```

### Monitoring

```bash
ai-cli -x "setup simple monitoring of disk space"
```

Может вернуть:
```bash
df -h | grep -v tmpfs
du -sh /* 2>/dev/null | sort -h
```

## Заключение

Выполнение команд с AI CLI - мощный инструмент для:
- 🎓 Обучения командам
- 🔍 Быстрой диагностики
- ⚡ Автоматизации рутины
- 📝 Документирования процессов

**Используйте ответственно и всегда проверяйте команды перед выполнением!**

---

**См. также:**
- `USAGE.md` - Общее руководство
- `CHEATSHEET.md` - Быстрая справка
- `FILE_GENERATION.md` - Генерация файлов
